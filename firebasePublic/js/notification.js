let notficationBox = document.querySelector('#notificationBox')



function parseNotificationBox(box) {
    var base = 0;
    for (var i =0; i<=box.length; i++_ {
        //notification of notes
        base+=1;
        for (var k=0; k<=base; k++) {
            k[i] +=3;
            console.log('view notes within notes');
        }
    }
    return base;


}
function checkNotification() {
    //parses on new update, do this when user logins to site
}


//Check on Note Discussion


//MVP features
function checkThread() {
    //forgot the comment variable syntax. Check later
    return `You recently got ${} on ${}`.format(amountOfCommentsMade)
}

//Check if students add new notes based on header
function checkNoteHeader() {

    return 'several other people mention this header';
}

function BadgeNotification(badgeInfo) {
    return `Congradulations! You unlocked ${}`.format(badgeInfo)
}

function createNewNotification(box) {
    return box;
}
